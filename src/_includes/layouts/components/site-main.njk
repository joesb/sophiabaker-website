    <main id="content" class="main">
			{% if not primary and not list %}
			<div class="site-content page{% if page.class %} {{ page.class }}{% endif %}">

				{{ content | safe }}
			</div>
			{% else %}

				{{ content | safe }}

			{% endif %}
		</main>